# Force everything except cobbler and nailgun dir to port 9000
RewriteEngine on
RewriteCond %{HTTPS} off
RewriteCond %{REQUEST_URI} !^/(cblr|cobbler)
RewriteRule (.*) http://%{HTTP_HOST}:9000%{REQUEST_URI} [R=301,L]
