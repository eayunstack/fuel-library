#!/bin/sh
. /etc/init.d/functions

cd /etc/sysconfig/network-scripts
. ./network-functions

[ -f ../network ] && . ../network

CONFIG=${1}

source_config

SCRIPT="/etc/sysconfig/network-scripts/interface-pre-up-script-$DEVICE"
if [[ -x $SCRIPT ]] ; then
. $SCRIPT
fi
